<% @rooms.each do |room| %>
  <div>
    <%= room.name %>
  </div>
  <div>
    <%= link_to 'room', room_path(room) %>
  </div>
<% end %>

<% if current_user.has_role? :admin %>
  <%= link_to 'new room', new_room_path %>
<% end %>